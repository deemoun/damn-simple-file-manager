<Window x:Class="DamnSimpleFileManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Damn Simple File Manager" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="LeftDriveSelector" Grid.Column="0" SelectionChanged="LeftDriveSelector_SelectionChanged" />
            <Label Grid.Column="1" Content="" Width="50" />
            <ComboBox x:Name="RightDriveSelector" Grid.Column="2" SelectionChanged="RightDriveSelector_SelectionChanged" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListView x:Name="LeftList" Grid.Column="0" DisplayMemberPath="Name" MouseDoubleClick="LeftList_DoubleClick" />
            <Button Grid.Column="1" Content=">>" Width="50" Margin="10" IsEnabled="False" />
            <ListView x:Name="RightList" Grid.Column="2" DisplayMemberPath="Name" MouseDoubleClick="RightList_DoubleClick" />
        </Grid>
    </Grid>
</Window>